server:
  port: 8081

#JDBC配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/t?serverTimezone=UTC
    username: zc
    password: 123456
    #整合数据库连接池
    # hikari:
  jpa:
    database: mysql
    show-sql: true

#      ddl-auto: update

  #mybatis:
  #mapper-locations: classpath:mapper/*.xml
  # type-aliases-package: com.qyq.pojo
